
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow public read/write access to all collections.
    // This makes all data public and accessible by any client.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
